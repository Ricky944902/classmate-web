// API配置
const config = {
  // 后端API地址
  // 本地开发环境
  // apiUrl: 'http://localhost:5000',
  // 生产环境（部署时需要替换为实际的后端地址）
  // 对于Vercel部署，地址通常为 https://<your-vercel-project-name>.vercel.app
  // 对于GitHub Pages，你需要使用CORS代理或部署后端到云服务
  apiUrl: 'https://<your-deployed-backend-api>',
};

export default config;